# TODO - Crop Disease Detector UI Fixes

- [ ] Update `app/app.py` to add a sidebar toggle (open by default) so user can close and reopen it.
- [ ] Wire the sidebar toggle state using `st.session_state.sidebar_open`.
- [ ] Update `app/app.py` layout so the toggle button appears when sidebar is closed.
- [ ] Improve color palette consistency in `app/style.css` (remove duplicated dark theme block, ensure vibrant professional colors).
- [ ] Align Plotly chart colors in `app/app.py` to the new palette.
- [ ] Run `streamlit run app/app.py` and visually verify sidebar + theme across all pages.

